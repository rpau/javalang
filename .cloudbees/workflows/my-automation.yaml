apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: Say hello
        uses: docker://maven:3.9.5-eclipse-temurin-11
        shell: sh
        run: |
          mvn test
